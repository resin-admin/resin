/*!!!
section: Spinner
description: To center a spinner within the site-body simply add the class `.centered` to the spinner like so `<div class="centered spinner"></div>`.
jade: .spinner
*/
.spinner
  @extend .fadeIn
  padding 30px
  display inline-block
  border-radius radius
  margin 0 auto
  background primaryColor
  &.centered
    position: absolute
    top: 50%
    left: 50%
    transform: translateX(-50%) translateY(-50%)
    margin-left: 150px 
  &:before
    content ''
    display block
    width 60px
    height 60px
    margin auto
    background-color white
    animation rotateplane 1.2s infinite ease-in-out
    border-radius radius
  &:after
    margin 20px auto 0 auto
    font-size 14px
    color white
    content 'loading...'
    display block
    font-family 'Lato', sans-serif
    text-align center
    overflow hidden
    width 46px
    animation ellipses 2s infinite linear

@keyframes ellipses
  0%, 24%
    width 46px
  25%, 49%
    width 49px
  50%, 74%
    width 52px
  75%, 100%
    width 58px

@keyframes rotateplane 
  0%  
    transform: perspective(120px) rotateX(0deg) rotateY(0deg)
  50%  
    transform: perspective(120px) rotateX(-180.1deg) rotateY(0deg)
  100%  
    transform: perspective(120px) rotateX(-180deg) rotateY(-179.9deg)
